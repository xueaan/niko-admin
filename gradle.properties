# 内存配置
org.gradle.jvmargs=-Xmx12g -Xms8g -XX:+UseG1GC -XX:MaxGCPauseMillis=200

# 并行编译
org.gradle.parallel=true

# 构建缓存
org.gradle.caching=true

# 配置缓存
org.gradle.configuration-cache=true

# 守护进程
org.gradle.daemon=true
